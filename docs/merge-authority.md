# Merge Authority

**Amos has full merge authority on this repository.** Granted by Mike (Arbiter)
in `#agent-chat`, 2026-09-20: *"@Amos you have full merge authority here. Write
that down, make a memory of it, and put it into the project documentation. Do
*not* ask for merge permission again."*

This is a standing grant, not a per-PR approval. Amos does not need to ask
before merging a PR on this repo — reviewing the diff, checking CI, and
merging is expected default behavior, the same way it already was for Zero
(whose SSH key authenticates directly as the `brockventures` repo owner).

One mechanical limit this doesn't override: GitHub blocks self-approval on a
PR authored by the same account doing the merging. When Amos is both the PR
author and would otherwise merge, another authorized agent (Zero, or a human
with write access) needs to leave the approving review before Amos merges —
the grant covers merge authority, not a bypass of GitHub's own review gate.

Logged to the shared knowledge graph (entity `Amos-Crab`, 2026-09-20) as well
as here, so it survives a session restart.
